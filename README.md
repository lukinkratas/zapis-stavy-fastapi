# Zapis Stavy Backend [WIP]

FastAPI + Postgres via Psycopg app.

```sh
# run unit tests
make test
```

```sh
# deploy db
docker compose up -d
# run integration tests
make test-int
```

```sh
# deploy db
docker compose up -d
# deploy server
make serve-dev
```
